to_nat(xs: String): Maybe<Nat>
  case xs{ 
    nil : Maybe.none<Nat>
    cons: Maybe.some<Nat>(to_nat.go(xs, 1))
  }
to_nat.go(xs: String, acum: Nat): Nat
  case xs {
    nil : acum
    cons: 
      case Char.is_digit(xs.head){
        false: acum
        true : to_nat.go(xs.tail, Nat.add(Nat.mul(acum, 10), to_digit(xs.head)))
      }
  }

to_digit(x: Char): Nat
  Nat.sub(U16.to_nat(x), 48)


to_nat: Maybe<Nat>
  let a = "1234"
  Maybe.show<Nat>(to_nat(a))
